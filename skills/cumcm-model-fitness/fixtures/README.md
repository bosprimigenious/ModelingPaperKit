# Fixtures

Shared structural sample: repository `artifacts/model_fitness.md` (create from template when running a live problem).

Machine check:

```bash
python3 scripts/check_model_fitness.py --artifacts artifacts
```
