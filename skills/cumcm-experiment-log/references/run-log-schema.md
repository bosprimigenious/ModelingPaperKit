# Run Log Schema

```text
run_id:
date:
script:
data_inputs:
preprocessing:
model:
parameters:
seed:
metrics:
outputs:
paper_claims:
notes:
```

Keep the schema in Markdown or CSV unless the team already has a better structured format.
